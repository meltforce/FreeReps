server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "localhost"
  port: 5432
  name: "freereps"
  user: "freereps"
  password: "freereps"
  sslmode: "disable"

tailscale:
  enabled: false       # set to true (default) for production/Docker
  hostname: "freereps"
  state_dir: "tsnet-state"
