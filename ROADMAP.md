# Roadmap

**This file contains open work only.** Every row carries the status token
`[open]`. Closed work is not struck through here — it is removed and lives in
[`DECISIONS.md`](DECISIONS.md) (decisions, with their reasoning) or
[`INCIDENTS.md`](INCIDENTS.md) (postmortems).

Columns: **Status** is always `[open]`. **Where** names the artifact the work
touches. **Trigger** carries the condition for items that are deliberately
deferred, and is empty for items that are simply pending. **Notes** carries the
reasoning.

Before closing an item, check its entry for residual work, dates or triggers —
each becomes its own `[open]` row before the entry is moved out.

## Dashboard

| Status | Item | Where | Trigger | Notes |
|---|---|---|---|---|
| `[open]` | The HAE ingest discards the device name, so iPhone and Watch share one source | `server/internal/ingest/health/provider.go`, `source_priority` | An export that carries both devices separately | A metric row stores `source = ''` for everything from Health Auto Export; the `sources[]` entry in the payload names the device and is dropped at `provider.go:161`. Priority therefore cannot separate a phone's step count from a watch's. The 5-minute window used to hide this by keeping one row per window; since [`DECISIONS.md`](DECISIONS.md), 2026-09-20 every row of the winning source is summed, so an export carrying both devices would count steps twice. Measured on the deployed instance on 2026-09-20: not the case here — all 552 Apple `step_count` rows over 30 days sit alone in their window. Storing the device name needs a decision on the existing rows, which keep `source = ''` and would rank against named devices. |
| `[open]` | `get_workouts` in the MCP misses the strength sessions the workout list merges | `server/internal/mcp/tools.go:314` | Alpha Progression is used again, or another provider delivers sessions that have no workout row | The tool calls `QueryWorkouts`; the screen calls `QueryWorkoutsMerged`, which additionally folds in the Alpha Progression set sessions, so an MCP consumer asking for a period that contains one gets a shorter list than the screen shows. **Deferred on 2026-09-22: Alpha Progression is no longer in use**, which leaves the two apart only for historical sessions. Switching the tool over needs a decision on the synthetic ids `QueryWorkoutsMerged` mints, because `GET /api/v1/workouts/{id}` cannot resolve them and the consumer would receive workouts it cannot look up — the same constraint the roadmap row on the detail view carries. |
| `[open]` | The iOS app cannot ask the server where its own last import ended | `server/internal/server/server.go`, `app/Sources/FreeReps/Services/SyncService.swift:393` | The app is reinstalled or moves to a new phone, so the local sync state is empty | After a completed full sync the app starts the next one at its stored anchor minus 7 days (`SyncService.swift:393`); that state lives in `UserDefaults` and is lost on reinstall, after which the first sync starts at the `backfillDays` setting. No existing endpoint can replace it: `GET /api/v1/import-logs` records the app's calls as `freereps_ios` since 2026-09-25, but returns the newest calls of any client without a filter, and `GET /api/v1/metrics/latest` returns the newest point of the winning source for visible metrics only — on 2026-09-25 `active_energy` reported `Oura` at 12:00Z, which says nothing about how far Apple Health data has arrived. The client identifier exists since 2026-09-25 (`X-FreeReps-Client`, [`DECISIONS.md`](DECISIONS.md)); what is missing is a stored last-successful-import time per client and an endpoint that returns it. Raised 2026-09-25 while the first iOS 27 full sync ran from 2000-01-01. |
| `[open]` | The iOS app re-sends the 7 days before its previous run on every sync | `app/Sources/FreeReps/Services/SyncService.swift`, `app/Sources/FreeReps/Services/HealthKitService.swift` | | Measured 2026-09-25: a sync from Siri posted 7,217 metric values of which 0 were new. The window exists because samples arrive in HealthKit with timestamps before the previous sync (Watch transfer, Oura writing the night later, a sleep stage spanning the sync), so a start-date filter would drop them. `HKAnchoredObjectQuery` returns what was added since a stored anchor regardless of timestamps, and deletions. Plan: keep the date-based backfill for the first run (the anchored query without a prior anchor skips samples for some types, per the comment in `HealthKitService.swift`), store one anchor per type only after the server confirmed the ingest, recompute only the hourly buckets the new samples touch for the 11 cumulative types, and re-send the last 24 hours once a day in case an anchor is lost. Depends on the metric upsert of 2026-09-25, since recomputed buckets have to replace stored ones. |
| `[open]` | Stored rows do not record which client delivered them | `server/internal/ingest/health/provider.go`, `server/specs/database-schema.md` | A question about which client delivered a given row, rather than a given call | Since 2026-09-25 `import_logs.source` separates the iOS app (`freereps_ios`) from Health Auto Export (`hae_rest`) per call. `health_metrics`, `workouts`, `sleep_stages` and `category_samples` carry no client column; the rows are told apart only indirectly — Health Auto Export rows have `source = ''` ([`DECISIONS.md`](DECISIONS.md), 2026-09-22), the app's rows carry the HealthKit `sourceDisplayName`. A client column on the hypertables needs a decision on the existing rows, which would stay empty. Since the upsert of 2026-09-25 ([`INCIDENTS.md`](INCIDENTS.md)) the last delivery for a key wins instead of the first: the iOS app and Health Auto Export both write hourly steps under `source = ''`, so with both running a bucket takes whichever client posted last, and it changes between them if they aggregate differently. |
| `[open]` | The metric table draws its sparklines in the frame tone | `server/web/src/pages/DashboardPage.tsx:315`, `components/dashboard/MetricRows.tsx:92` | | The chart rule of 2026-09-20 ([`DECISIONS.md`](DECISIONS.md)) puts measured series on the data ramp and leaves frame and derived references neutral. Both sparklines in the Today metric table — the 180×22 one on desktop and the 90×20 one in the phone rows — draw in `--color-neutral-500`, the frame tone, although they carry the same series the hero sparklines now take on `--color-data-3`. The Trends small multiples were moved from exactly that tone in the same pass. The design handoff `design_handoff_datenfarben` names neither file across its three packages, so the density of the table view may be the reason to keep them quiet; the decision is whether a dense repeated sparkline counts as a series or as frame. Found 2026-09-20 while applying the third package. |
| `[open]` | Persist the selected time range per screen | `server/web/src/`, `server/internal/storage/preferences.go` | | Metric selection and the four hero numbers persist in `user_preferences`; the range does not, and resets to each screen's default on a fresh session. The table and the `GetPreference`/`SetPreference` pair are already in place — this is one more key. |
| `[open]` | Manual entry for muscle mass | `server/web/src/`, `server/internal/server/`, `server/internal/storage/health_metrics.go` | | Measured once a year at the doctor, and that measurement is considerably more precise than the scale's impedance estimate. The metric `muscle_mass` already exists in the allowlist (migration `000026`), so this is an entry form plus a write path, not a schema change. **It needs a source name of its own** — writing it under `Withings` would put the annual measurement and the scale reading in the same priority bucket, where the winner per five-minute bucket is undefined; a distinct name ranked above `Withings` in `source_priority` resolves it. The conflict is dormant today because the scale's body composition series ends 2026-04-11, and it returns the moment the impedance scale is used again. |
| `[open]` | Lag correlation over the full series, server-side | `server/internal/storage/health_metrics.go`, `server/web/src/pages/CorrelationPage.tsx` | Windows beyond one year | The Correlations screen fetches both metrics as daily series and pairs them in the browser to compute r at four lags from one payload. At a 1y window that is two arrays of 365; beyond that the pairing belongs in SQL. |
| `[open]` | The Oura sync repeats a full backfill window for `vo2_max` on every cycle | `server/internal/oura/sync.go:119` |  | `GetVO2Max` answers HTTP 404 for this ring model, measured against the live API on 2026-09-21. `syncDataType` treats 404 as expected and returns before `UpsertOuraSyncState`, so `oura_sync_state` holds no `vo2_max` row and the next cycle computes `startDate` from `BackfillDays` again. Measured on the deployed instance on 2026-09-21: nine of the ten data types have a state row, `vo2_max` has none, at one request per 30 minutes. The same shape applies to any endpoint that begins answering after a period of 404s — it would then fetch the whole backfill window in one cycle. Decide between recording the attempt and skipping a type whose endpoint answered 404. |
| `[open]` | `active_energy` summed from `health_metrics` disagrees with the activity summary | `server/internal/storage/health_metrics.go:444`, the activity summary path | | Measured on the deployed instance on 2026-09-23, per UTC day, `get_health_metrics` against `get_activity_summaries`: 2026-09-14 1,020 kcal against 475, 2026-09-15 875 against 868, 2026-09-16 513 against 485, 2026-09-17 630 against 603, 2026-09-18 1,284 against 6; 2026-09-19 and 2026-09-20 have no summary. A UTC day boundary against local-time summaries shifts figures between neighbouring days and does not explain 1,020 against 475. Unchecked cause candidates: two devices under the shared `source = ''` of the HAE path, summed since every row of the winning source counts (see the device-name row above, which measured `step_count` only); or the summary missing a period. It matters beyond the dashboard because the MCP descriptions now state that `avg` is a total for cumulative metrics, so a model reports the summed figure without the plausibility check it applied before. Found 2026-09-23 in the A/B probe of the tool descriptions. Decide which figure is correct before relying on either. The figures above predate the metric upsert of 2026-09-25 ([`INCIDENTS.md`](INCIDENTS.md)), which changed stored `active_energy` for Oura on 67 of the last 90 days and for the iOS app wherever a sync ran inside an hour; measure again before looking for a cause. |

## Training data

| Status | Item | Where | Trigger | Notes |
|---|---|---|---|---|
| `[open]` | Tonnage ignores body weight | `server/internal/storage/training_metrics.go`, `training_summary.go`, `training_volume.go`, `training_intensity.go`, `exercise_templates` | The exercise mix changes, so that tonnage is compared across periods that log body-weight work differently | **Deferred on 2026-09-20: the effort sits in the factor per exercise, not in the weight source.** `SUM(weight_kg * reps)` counts external load only. Measured on the deployed instance on 2026-09-20: of 3,483 working sets, 505 are body-weight-plus and a further 25 carry neither weight nor flag; total tonnage is 1,374,616 kg. **The open decision is the factor per exercise, not the weight source.** Adding one whole body weight to every flagged set adds 407,243 kg, 30 % — but 64 % of that comes from Hanging Leg Raises, Decline Crunches and Hyperextensions on Roman Chair, where the body does not move as one mass; restricting the whole body weight to chin-ups, pull-ups and dips adds 115,274 kg, 8 %. Fifteen exercise names cover every flagged set, so the factor fits in a seed. The weight itself is available: `weight_body_mass` holds 331 distinct days since 2024-01-02, and the largest gap between a session and the nearest measurement is 7 days; 312 of 368 rows arrive over the Apple Health path and 56 through the Withings integration, so a source decision applies here as well. A user whose scale delivers nothing needs a stored figure, in the shape `PrefBirthDate` and `PrefMaxHeartRate` already have. Residual: the formula stands in five places (`training_metrics.go:44`, `training_summary.go:94`, `training_volume.go:83`, `training_intensity.go:134` and `:165`), and `RebuildTrainingMetrics` materialises `strength_tonnage` into `health_metrics`, so a change rewrites the stored series and every correlation computed from it. The figures recorded here until 2026-09-20 (827 sets, 672,742 kg, 31 %) were taken on 2026-08-05 and counted the duplicated Alpha sessions that `3727475` and `4139d01` removed on 2026-08-10. |
| `[open]` | Planning project | outside this repo | | Separate Claude Code project with both MCP servers, permissions denying the Hevy analysis tools. See [`DECISIONS.md`](DECISIONS.md), 2026-08-04. |
| `[open]` | Backfill the Alpha history into Hevy | outside this repo | | Roughly the last 8 to 12 weeks via `POST /v1/workouts`, so the app shows previous-session values from day one. Set `is_private` on the created workouts. The ingest cutoff in `hevy_credentials.sync_from` keeps them from flowing back. |
| `[open]` | Source deduplication in the training aggregates | `server/internal/storage/training_summary.go`, `training_intensity.go` | Two sources cover one period | Both queries sum across `workout_sets` without filtering on `source`. The cutoff currently keeps the periods disjoint; if they ever overlap, tonnage and set counts double. Same failure shape as [`INCIDENTS.md`](INCIDENTS.md), 2026-03-26. |
| `[open]` | `Bench Press` names two different exercises | `server/internal/storage/training_metrics.go`, `exercise_name_map` | | Barbell through 2026-06-23, dumbbells from 2026-07-03 at 30 kg per hand. The estimated-1RM series for that exercise mixes both, so the drop at the changeover reads as lost strength rather than a different lift. Alpha writes the equipment into its own column, which is what separates the two; the exercise name alone does not. Found 2026-08-10 alongside [`INCIDENTS.md`](INCIDENTS.md), 2026-08-10, and deliberately left out of that change. |
| `[open]` | 2026-05-02 holds nine sets, all flagged as warm-ups | `server/internal/storage/training_summary.go`, `training_volume.go`, or the data | | Four exercises, each ramping in weight, with no working set after them — either an abandoned session or a logging gap. Every aggregate filters on `NOT is_warmup`, so the day contributes nothing and appears as a rest day; the session is not visible as an anomaly anywhere. Decide whether it is data to correct or a case the aggregates should surface, and do not fix it by relaxing the warm-up filter, which would inflate every other period. Found 2026-08-10. |
| `[open]` | Detail view for sessions without a workout row | `server/web/src/pages/WorkoutDetailPage.tsx` | | Alpha and Hevy sessions render from route state because `GET /api/v1/workouts/{id}` cannot resolve a synthetic id. Reloading such a page loses the data. Hevy now supplies a stable `external_id` to resolve against. |
